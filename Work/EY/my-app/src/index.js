import react from 'react'; 
import ReactDOM from 'react-dom'; 
import App from './App'; 

// this is to render the app inside the root element inside the HTML page.
ReactDOM.render(<App/>, document.getElementById('root')); 
